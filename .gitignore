target/
dist/
logs/
.DS_Store
.env
test-config.yaml
config.test.yaml
config.yaml
docker-compose.override.yml
config.control-plane-greenfield.yaml
config.yaml.managed-auth.d/
output/
.codex/

# Frontend
web/node_modules/
web/dist/
web/test-results/
web/tsconfig.tsbuildinfo
.playwright-cli/

# Claude Code — track settings/commands, ignore transient data
.claude/*
!.claude/settings.json
!.claude/commands/

# OpenCode — track commands
.opencode/*
!.opencode/commands/

# Agent Skills — track for Codex/Copilot/OpenCode discovery
# Skills live in .agents/skills/ (not .claude/skills/) to avoid Claude Code duplicate discovery
!.agents/
